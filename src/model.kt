
class Model(var root: Entity = Entity("",null)) {

    // here I need observer

/*
    fun addAttribute(entity:Entity,text: String, innerText:String){
        entity.addAttributes(text, innerText)
        notifyObservers{}
    }

    fun removeAttribute(text: String, innerText:String){
        ent.removeAttributes(text, innerText)
        //notifyObservers{}
    }
*/


}
